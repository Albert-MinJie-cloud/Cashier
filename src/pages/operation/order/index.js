import React from 'react';
import Menu from "../../../components/menu";

function Order(props) {
    return (
        <div className = {'order'}>
            <Menu num = {3}/>
            3
        </div>
    );
}

export default Order;